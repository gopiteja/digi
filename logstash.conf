input {
  gelf {}
}

filter {
  grok {
    match => {"message" => "%{TIMESTAMP_ISO8601:timestamp} - %{WORD:trace-id} - %{WORD:log-level} - %{WORD:tenant-id} - %{GREEDYDATA:message}"}
  }
  date {
    match => ["timestamp", "ISO8601"]
  }
}

output {
  stdout {}
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "basic_log"
  }
}